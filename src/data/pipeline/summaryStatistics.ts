import type { DelimitedText, StepCallBack } from "../model";

export const summaryStatistics = async (
    delimiter: string,
    acumulator: Uint32Array[][],
    setCallBack: StepCallBack
): Promise<DelimitedText[]> => {
    return [];
}